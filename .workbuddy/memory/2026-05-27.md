# 2026-05-27 工作日志

## UI/UX 美化与性能优化（上下文恢复续）

### 已完成的工作（来自摘要）
- `src/index.css`：色彩体系升级（品牌蓝 hsl(222 89% 55%)）、玻璃拟态 `.glass-card`、新增7种工具类、新增关键帧
- `src/App.tsx`：Hero 动态背景、渐变标题、滚动 Header、移动端 Tab 导航、`AmbientBackground` 组件
- `src/features/gallery-page.tsx`（第一轮）：统计卡片分色、图片卡片优化、分页改版
- `vite.config.ts`：esbuild 压缩、5路分包、资源分目录、Tree Shaking

### 本次续做（Task #6）

#### IntersectionObserver 懒加载实现
**文件**：`src/features/gallery-page.tsx`

**新增 `useLazyImage` Hook**（约50行）：
- 状态机：`idle → loading → loaded | error`
- `rootMargin: '200px'` 提前200px开始加载
- 前6张卡片（above-the-fold）`eager=true` 直接进入 loading 状态，无需 IO 监听
- 每个状态消费后断开 observer，避免内存泄漏

**新增 `ImageCard` 独立组件**：
- 骨架屏占位（`skeleton-shimmer`），在 `idle/loading` 时展示
- 图片 `opacity: 0→1` 淡入（`state === 'loaded'` 时 `opacity-100`）
- 加载失败时显示图标 + "加载失败"文字
- 废弃 `loading="lazy"` 属性，改为精细化 IO 控制
- 重构删除逻辑：接收 `isDeleting` prop 用于 disabled 状态

**构建验证**：TypeScript 0错误，生产构建成功，CSS 78.65 kB (gzip 13.87 kB)

### 部署（Task #7）
- 尝试 `edgeone pages deploy -n tcpaiii` → 失败：项目绑定 GitHub Provider，不支持文件夹直传
- 尝试 preview 环境 → 相同限制
- MCP edgeone-pages 工具两次超时
- **建议**：通过 GitHub 推送触发自动部署，或在 EdgeOne 控制台创建新的 Upload 类型项目
